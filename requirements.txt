# ── Core (required) ───────────────────────────────────────────
numpy>=1.21.0
scikit-learn>=1.0.0
pandas>=1.3.0

# ── Real benchmark datasets (optional) ───────────────────────
# Uncomment to load MedQA-USMLE, MedMCQA, TruthfulQA
# pip install datasets
# datasets>=2.0.0

# ── Real LLM hidden states (optional — production mode) ───────
# Uncomment for real transformer backbone integration
# pip install transformers torch
# transformers>=4.30.0
# torch>=2.0.0

# ── INT4 quantization (optional — memory-efficient mode) ──────
# pip install bitsandbytes
# bitsandbytes>=0.41.0

# ── Medical NER (optional — better entity extraction) ─────────
# pip install scispacy
# python -m spacy download en_core_sci_lg

# ── AI API clients (optional — for automatic testing) ─────────
# pip install openai anthropic
# openai>=1.0.0
# anthropic>=0.20.0

# ── Development / Testing ─────────────────────────────────────
# pytest>=7.0.0
# black>=23.0.0
# isort>=5.0.0
# flake8>=6.0.0
